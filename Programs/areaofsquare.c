#include<stdio.h>
int main()
{
    int area, length;
    printf("enter the length of square:");
    scanf("%d",&length);
    area= length* length;
    printf("the area of square :%d",area);
    return 0;
}